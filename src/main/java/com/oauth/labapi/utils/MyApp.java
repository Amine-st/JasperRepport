/*

package com.oauth.labapi.utils;


import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.statemachine.StateMachine;
import org.springframework.stereotype.Component;

@Component
class  MyApp implements CommandLineRunner {


    @Autowired
    private StateMachine<States,EventsF> stateMachine;



    @Override
    public void run(String... args) throws Exception {
        stateMachine.sendEvent(EventsF.DEPOT_FACTURE);
        stateMachine.sendEvent(EventsF.ST_REJET_DU_SERVICE_FAIT);
        stateMachine.sendEvent(EventsF.ST_SATISFACTION_DU_SERVICE_FAIT);
        stateMachine.sendEvent(EventsF.ST_REJET_DU_SERVICE_FAIT);
        stateMachine.sendEvent(EventsF.ST_SATISFACTION_DU_SERVICE_FAIT);
        stateMachine.sendEvent(EventsF.ST_VALIDATION_DU_SERVICE_FAIT);
        stateMachine.sendEvent(EventsF.REJET_DOSSIER_DE_PAIEMENT_TECHNIQUE);
        stateMachine.sendEvent(EventsF.ST_SATISFACTION_DU_REJET_DU_DOSSIER_DE_PAIEMENT);
        stateMachine.sendEvent(EventsF.SM_EMISSION_DE_DOSSIER_DE_PAIEMENT);
        stateMachine.sendEvent(EventsF.ORDONNANCEMENT_DU_DOSSIER_DE_PAIEMENT);
        stateMachine.sendEvent(EventsF.PAIEMENT);

    }
}

*/
